package com.group.findit.ui.domain.model

/**
 * Domain model representing a photo in the game.
 * @property name The name associated with the photo.
 * @property image The image data as a string (e.g., URL or base64).
 */
data class Photo(val name: String, val image: String)
